import React from 'react';
import { View } from 'react-native';
import RecipeScreen from '../../components/ui/screen/recipeScreen';

export default function RecipeDetailPage() {
  return (
    <View style={{ flex: 1 }}>
      <RecipeScreen />
    </View>
  );
}